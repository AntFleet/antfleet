# CLAUDE.md

The working agreement for coding agents in this repo lives in `AGENTS.md`.
Read it before making changes — especially the **Git Workflow** section
(isolated `/tmp` worktrees, one branch per task, branch → PR → green CI → merge),
which exists because concurrent sessions share this checkout.

@AGENTS.md
