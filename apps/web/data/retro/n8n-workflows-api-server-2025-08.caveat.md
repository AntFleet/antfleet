On 2025-08-15, the n8n-workflows `api_server.py` file-download fix introduced the surface later tracked as CVE-2025-55526: untrusted workflow filenames were joined into filesystem paths without a containment check. AntFleet re-ran the two-provider retro scan blind against the introducing commit (`ee254131`). The prompt label was deliberately neutral: "n8n-workflows api_server.py file download fix" — no "CVE", no "path traversal", no "directory traversal". Both providers still found the bug class cold.

The unanimous gate fired. Claude Opus 4.7 flagged a path-traversal risk in `api_server.py`, citing the same download/detail/diagram path joins and recommending `Path.resolve()` containment. GPT-5 independently graded the issue HIGH/HIGH and named the Windows backslash bypass Opus did not enumerate: `..\README.md` can resolve outside `workflows` on Windows even when FastAPI's default path segment blocks literal forward slashes. That overlap is the conservative unanimous finding in the receipt.

The two-model spread also mattered beyond the overlap. Opus produced unique findings for wildcard CORS with credentials, an FTS5 query built from raw filename input, global exception leakage, a Content-Disposition concern, deprecated startup wiring, and category matching bugs. GPT-5 separately called out the unauthenticated cross-origin reindex endpoint, an integrations API contract gap, the category substring bug, and a README path typo. Counting the overlapping path-traversal issue once, the run produced roughly eleven unique issues across both providers.

Attribution is intentionally weaker than the Moonwell, OpenClaw, and Zcash Orchard retros. The n8n-workflows introducing commit (`ee254131`) does not carry a public `Co-Authored-By: <AI>` trailer. The "AI-coded" classification is second-hand: CSA Lab Space's March 2026 research note on AI-generated-code vulnerability surge catalogs 74 confirmed AI-generated CVEs, including this case, using attribution methodology that is not public to us: https://labs.cloudsecurityalliance.org/research/csa-research-note-ai-generated-code-vulnerability-surge-2026/. Treat that as a caveat. This receipt's value is the technical result: a blind, neutral-label, two-provider unanimous gate on a real CVE, with the attribution caveat kept explicit.
