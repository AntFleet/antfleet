node_modules/
dist/
coverage/
.omc/
!.omc/
.omc/*
!.omc/research/
.omc/research/*
!.omc/research/evidence-bundle-bench-report.md
!.omc/research/evidence-bundle-launch-tweet.md
.claude/
.DS_Store
.env
.env.local
.env.*.local
# Backups/variants of local env files (e.g. .env.local.prod-bak.<ts>) — these
# carry real prod secrets and must never be stage-able.
.env.local.*
.env*.bak
.env*~
*.pem
.fleet/runs/
.fleet/findings/
.fleet/patches/
.fleet/reports/
.fleet/locks/
examples/dogfood-results/[0-9]*.md
# Real-repo corpus source files live on disk for local spike runs only.
# They are downstream code and do not belong in this tree. GROUND_TRUTH.md
# names the exact paths a contributor needs to populate locally.
examples/antseed-corpus/apps/

.vercel
.env.vercel*

# sting is a separate product repo (github.com/AntFleet/sting) that sometimes
# sits side-by-side on operator laptops at ./sting. It must NEVER be vendored
# into this repo. The historical `.git/info/exclude` entry is machine-local and
# would not protect a teammate who runs `git add .` on a fresh clone. See
# audit/DECISION-sting-home.md (T2.5).
/sting/

# Internal security audit + remediation prompts/reports — operator-only, never
# publish or commit (findings are exploitable on the live service).
audit/

# ACP CLI event scratch (local round-trip smoke)
apps/web/.acp/

# §2 kill-test artifacts: labeled target codebases + run reports stay local.
solidity-killtest/targets/
solidity-killtest/results/

# Session handoff prompts (operator-internal orchestration)
src/sidecar-solidity/BUILD_PROMPT.md
src/sidecar-solidity/SPEC_WRITING_PROMPT.md
